name: build tar

on: workflow_call


jobs:
  build:
    name: Generate App Bundle
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Get app info
        id: app
        run: |
          app_id=$(cat TA-*/app.manifest | jq -r '.info.id.name')
          app_version=$(cat TA-*/app.manifest | jq -r '.info.id.version')
          echo "name=${app_id}" >> $GITHUB_OUTPUT
          echo "version=${app_version}" >> $GITHUB_OUTPUT
        working-directory: ./packages

      - name: Bundle app source
        run: |
          mkdir -p build
          COPYFILE_DISABLE=1 tar -C packages -zcvf build/${{ steps.app.outputs.name }}-${{ steps.app.outputs.version }}.tgz .
  
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: packaged_app
          path: build/*
